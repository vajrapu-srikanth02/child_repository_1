
name: Child Workflow 1

on:
  repository_dispatch:
    types: [trigger-workflow]

jobs:
  run-child-1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Run some task in Child 1
        run: echo "Running task in Child 1"
